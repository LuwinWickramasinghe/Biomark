import 'package:flutter/material.dart';

const kPrimaryColor =
    Color(0xFF0D47A1); // Dark Blue (for buttons, highlights, etc.)
const kPrimaryLightColor =
    Color(0xFF42A5F5); // Light Blue (for form backgrounds, etc.)

const double defaultPadding = 16.0;
